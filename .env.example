# =============================================================================
# Nursing Layoff Radar - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# For production, use: openssl rand -base64 16 to generate secure values
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Postgres connection string
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://nlr:nlr@localhost:5432/nlr

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Port the API server listens on (default: 8787)
PORT=8787

# -----------------------------------------------------------------------------
# Security Configuration
# -----------------------------------------------------------------------------
# REQUIRED: Access passcode for dashboard authentication
# Generate a secure passcode: openssl rand -base64 16
# WARNING: Never commit real passcodes to version control!
ACCESS_PASSCODE=

# JWT-like token secrets (generate strong random strings)
TOKEN_SECRET=
REFRESH_SECRET=

# Access token lifetime in minutes (default: 15)
ACCESS_TOKEN_TTL_MIN=15

# Refresh token lifetime in days (default: 7)
REFRESH_TOKEN_TTL_DAYS=7

# CORS allowed origins (comma-separated)
# For production, list your frontend domain(s)
# Example: https://dashboard.example.com,https://www.example.com
CORS_ORIGINS=

# Audit log output path
AUDIT_LOG_PATH=./logs/audit.log

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------
# Limit worker to specific states (comma-separated, e.g., IN,NY,OR)
# Leave empty to fetch all states
STATES=

# Nursing program directory sources
# Override these if the source URLs change or require specific query params.
CCNE_STATE_URL_TEMPLATE=https://directory.ccnecommunity.org/reports/rptAccreditedPrograms_New.asp?state={STATE}
CHEA_ACEN_URL=https://www.chea.org/acen-accredited-programs
# Optional: ACEN directory URL if you have a scrape-friendly listing.
ACEN_DIRECTORY_URL=
# Optional: CNEA directory URL if a different listing is preferred.
CNEA_DIRECTORY_URL=https://cnea.nln.org/accredited-programs

# -----------------------------------------------------------------------------
# Production Deployment Notes
# -----------------------------------------------------------------------------
# 1. Generate a strong passcode: openssl rand -base64 16
# 2. Set CORS_ORIGINS to your actual frontend domain(s)
# 3. Use a managed PostgreSQL service or secure your DB
# 4. Never expose DATABASE_URL or ACCESS_PASSCODE publicly
# 5. Use Cloudflare Tunnel or similar to avoid exposing server IP
# =============================================================================
